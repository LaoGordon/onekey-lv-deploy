--- a/livox_ws/src/livox_ros_driver2/CMakeLists.txt
+++ b/livox_ws/src/livox_ros_driver2/CMakeLists.txt
@@ -118,7 +118,7 @@ endif()
   # get include directories of custom msg headers
   if(HUMBLE_ROS STREQUAL "humble")
     rosidl_get_typesupport_target(cpp_typesupport_target
-    ${LIVOX_INTERFACES} "rosidl_typesupport_cpp")
+      ${LIVOX_INTERFACES} "rosidl_typesupport_cpp")
     target_link_libraries(${PROJECT_NAME} "${cpp_typesupport_target}")
   else()
     set(LIVOX_INTERFACE_TARGET "${LIVOX_INTERFACES}__rosidl_typesupport_cpp")
@@ -126,6 +126,10 @@ endif()
     get_target_property(LIVOX_INTERFACES_INCLUDE_DIRECTORIES ${LIVOX_INTERFACE_TARGET} INTERFACE_INCLUDE_DIRECTORIES)
   endif()
 
+  # Fallback: if HUMBLE_ROS is not set, set include directories
+  if(NOT HUMBLE_ROS OR NOT LIVOX_INTERFACES_INCLUDE_DIRECTORIES)
+    set(LIVOX_INTERFACES_INCLUDE_DIRECTORIES "${CMAKE_CURRENT_BINARY_DIR}/rosidl_generator_cpp/${LIVOX_INTERFACES}")
+  endif()
+
   # include file direcotry
   target_include_directories(${PROJECT_NAME} PUBLIC
     ${PCL_INCLUDE_DIRS}
